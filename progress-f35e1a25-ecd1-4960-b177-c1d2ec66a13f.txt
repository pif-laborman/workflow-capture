# Progress Log
Run: f35e1a25-ecd1-4960-b177-c1d2ec66a13f
Task: Parallel Antfarm Dispatch - convert dispatcher from sequential to parallel execution
Started: 2026-04-11

## Codebase Patterns
- antfarm-dispatch.py has hyphens in filename; import with importlib.util.spec_from_file_location
- ROLE_CONFIG dict defines per-role claude CLI args, user, and timeout; always deep-copy cmd list before mutating
- Agent execution uses `su - ralph -c '...'` pattern for ralph user roles
- Prompt is written to a NamedTemporaryFile with delete=False, chmod 0o644
- Tests use pytest with unittest.TestCase style; mock subprocess.Popen for agent tests
- Python 3.12.3; yaml and requests are available as imports
- Supabase creds resolved from env or pif-creds CLI fallback at module load time

---

## 2026-04-11 - US-001: Non-blocking execute_agent and process registry
- Added `PROCESS_REGISTRY: dict = {}` at module level after logging section
- Added `execute_agent_async()` that returns `{popen, stdout_path, prompt_file}` or None
- Stdout captured to temp file (not pipe) to avoid deadlock
- Prompt file preserved for caller to clean up after harvest
- Existing `execute_agent()` left unchanged
- Files changed: scripts/antfarm-dispatch.py, scripts/test_execute_agent_async.py, .gitignore
- **Learnings:** Module import requires patching env vars for SUPABASE_URL/KEY before exec_module
---

## 2026-04-11 - US-002: Harvest function for completed processes
- Added `AGENT_TIMEOUT_SECONDS = 1200` constant
- Added `harvest()` function that polls PROCESS_REGISTRY and runs completion logic
- Success path: sb_select step info, loop/verifyEach skip logic, handle_retry_step, antfarm_complete, check_milestone, runCompleted handling (notify, content-factory delivery, worktree cleanup, evaluator)
- Failure path: antfarm_fail, runFailed handling (notify, worktree cleanup, evaluator)
- Timeout path: kill process after 1200s, treat as failure via antfarm_fail
- Temp files (prompt_file, stdout_path) cleaned up after every harvest
- Returns count of harvested processes
- Files changed: scripts/antfarm-dispatch.py, scripts/test_harvest.py
- **Learnings:** Use `list(PROCESS_REGISTRY.items())` to iterate a snapshot when mutating during iteration; mock module functions with `patch.object(_mod, "fn_name")` pattern
---

## 2026-04-11 - US-003: Concurrency cap and spawn pass
- Added `PER_RUN_CAP = 1` constant for fair sharing across runs
- Added `get_max_concurrent()` — queries Supabase `policies` table for `max_concurrent_antfarm_agents`, defaults to 3
- Added `spawn_pass(run_id_filter=None)` — scans running runs, enforces global + per-run caps, peeks/claims/builds prompt/spawns agents, adds to PROCESS_REGISTRY
- spawn_pass calls prune_stale_worktrees() at start and close_stale_runs() at end
- Handles spawn failures (execute_agent_async returns None) by calling antfarm_fail
- Preserves worktree safety check and claim mismatch detection from dispatch_once
- Files changed: scripts/antfarm-dispatch.py, scripts/test_spawn_pass.py
- **Learnings:** spawn_pass mirrors dispatch_once's per-run loop structure but with capacity checks instead of blocking execution; re-check caps inside inner agent loop since spawning changes registry size
---

## 2026-04-11 - US-004: Daemon mode with adaptive polling
- Added `SHUTDOWN_REQUESTED = False` module-level flag for graceful shutdown coordination
- Added `daemon_loop(run_id_filter)` — long-running loop: harvest → spawn_pass → adaptive sleep
- Adaptive interval: 10s when PROCESS_REGISTRY non-empty, 60s when idle; sleeps in 1s increments checking SHUTDOWN_REQUESTED
- Added `--daemon` flag parsing in main(); daemon mode adds StreamHandler for journald visibility
- Existing `--once` and continuous loop modes unchanged
- Files changed: scripts/antfarm-dispatch.py, scripts/test_daemon_loop.py
- **Learnings:** When testing sleep-based loops, set SHUTDOWN_REQUESTED inside the mock sleep callback (not during spawn) to let the sleep accumulation actually run; patching `_mod.time` replaces the module reference so `time.sleep` in daemon_loop uses the mock
---

## 2026-04-11 - US-005: SIGTERM handler and graceful shutdown
- Added `import signal` to module imports
- Added `handle_sigterm(signum, frame)` that sets SHUTDOWN_REQUESTED=True and logs the event
- Registered SIGTERM handler in `daemon_loop()` before main loop via `signal.signal(signal.SIGTERM, handle_sigterm)`
- After main loop exits: 120s grace period with harvest() every 5s, then .kill() + final harvest for stragglers
- Fixed existing `test_adaptive_interval_active` to provide `mock_time.time` side_effect for new shutdown path
- Files changed: scripts/antfarm-dispatch.py, scripts/test_daemon_loop.py, scripts/test_sigterm_handler.py
- **Learnings:** When patching `_mod.time` as a MagicMock, any new code that calls `time.time()` in the same function will get a MagicMock return — must explicitly set `mock_time.time` with a side_effect iterator that returns increasing values to avoid infinite loops in time-comparison while loops
---

## 2026-04-11 - US-006: Startup recovery for orphaned steps
- Added `recover_orphaned_steps()` function that queries antfarm_steps for status='running' and resets them to 'pending'
- Loop steps (type='loop') preserve `current_story_id` in the reset so they resume the right story
- Called at start of `daemon_loop()` before entering the poll loop
- Returns count of recovered steps; logs each recovery with step_id and run_id
- Updated existing daemon_loop and sigterm tests to mock `recover_orphaned_steps` (added as outermost decorator)
- Files changed: scripts/antfarm-dispatch.py, scripts/test_recover_orphaned_steps.py, scripts/test_daemon_loop.py, scripts/test_sigterm_handler.py
- **Learnings:** When adding a new function call to daemon_loop, all existing tests that call daemon_loop need the new function mocked — add `@patch.object(_mod, "recover_orphaned_steps", return_value=0)` as outermost decorator and corresponding param at end of test method signature
---

## 2026-04-11 - US-007: Schedule disable on daemon startup and --once update
- Added `set_schedule_enabled(False)` call in `daemon_loop()` after `recover_orphaned_steps()` and before poll loop, with log message
- Rewrote `--once` code path to use spawn+harvest: calls `spawn_pass()`, loops `harvest()` + `sleep(5)` while `PROCESS_REGISTRY` non-empty, exits after all harvested
- Rewrote default continuous mode to use `harvest()` + `spawn_pass()` instead of `dispatch_once()`, exits when no active agents and no running runs
- Updated all daemon_loop tests in test_daemon_loop.py and test_sigterm_handler.py to mock `set_schedule_enabled`
- Removed old `test_once_flag_still_works` test that called `dispatch_once` directly
- Files changed: scripts/antfarm-dispatch.py, scripts/test_daemon_loop.py, scripts/test_sigterm_handler.py, scripts/test_schedule_disable_once.py (new)
- **Learnings:** Same pattern as US-006 — adding a new call to daemon_loop requires updating all existing daemon_loop test decorators. Added `@patch.object(_mod, "set_schedule_enabled")` alongside `recover_orphaned_steps` in every daemon_loop test
---
